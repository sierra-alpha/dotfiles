# -*- mode: toml -*-

# Kianga config file

[pull-repos]

    [pull-repos.dotfiles]
        url = git@github.com:sierra-alpha/dotfiles.git
        local = ~/dotfiles
        remote = origin
        branch = master

    [pull-repos.matapihi]
        url = git@github.com:sierra-alpha/kaianga.git
        local = ~/dev/matapihi
        remote = origin
        branch = master

    [pull-repos.kaianga]
        url = git@github.com:sierra-alpha/kaianga.git
        local = ~/dev/kaianga
        remote = origin
        branch = master